# Worklog: Migrate spec-generation tool into machinen as `derive`

## Context / Brief

We are migrating the spec-generation tool from the `machinen-experiments_specs` repo into the `machinen` monorepo as a new package called `derive`. The tool watches Claude Code conversation JSONL files, extracts behavioral intent, and generates Gherkin specs per git branch. It will also house test generation from those specs in the future.

The name `derive` captures the core action: derive specs from conversations, derive tests from specs. The tool sits alongside the existing packages (`bridge`, `supervisor`, `dtu-github-actions`, `ui`) in the pnpm workspace.

The original worklog for the tool's development lives at `machinen-experiments_specs/.notes/justin/worklogs/2026-03-03-machinen-setup.md`.

---

## RFC: Migrate specs package into machinen as `derive`

### 2000ft View

We copy the `specs/` package from the `machinen-experiments_specs` repo into the `machinen` monorepo as `derive/`. We rename the package, align it with monorepo conventions (tsgo, oxlint, oxfmt, vitest), register it in the pnpm workspace, and move the original worklog into this repo's `.notes/` directory.

### Implementation Breakdown

```
[NEW]    derive/                          — new package directory
[COPY]   derive/src/*.ts                  — all 6 source files from specs/src/
[COPY]   derive/tsconfig.json             — from specs/tsconfig.json
[MODIFY] derive/package.json              — rename from "machinen" to "derive",
                                            update typecheck script to use tsgo
[MODIFY] pnpm-workspace.yaml             — add "derive" to packages list
[COPY]   .notes/justin/worklogs/2026-03-03-machinen-setup.md
                                          — original tool development worklog
[SKIP]   probe-real-sysprompt.mjs         — diagnostic script, not carried over
[SKIP]   pnpm-lock.yaml                   — regenerated by pnpm install at root
```

### Monorepo alignment changes

- `typecheck` script: `tsgo` (not `tsc --noEmit`)
- No `start` script needed yet (daemon mode is not the primary use case in this repo)
- Remove `tsx` from devDependencies (available at root)
- Remove `typescript` from devDependencies (tsgo used instead)

### Behaviour Spec

```
GIVEN the derive package is added to the monorepo
WHEN pnpm install is run at the root
THEN derive's dependencies are installed and linked

GIVEN derive's source files are copied
WHEN pnpm --filter derive typecheck is run
THEN it passes cleanly
```

### Invariants

- No source file content changes — we copy as-is and only adjust package metadata.
- The package name in package.json is `derive`.
- The original worklog is preserved verbatim.

### Tasks

- [x] Copy source files from specs/src/ into derive/src/
- [x] Copy and adapt package.json (rename, align scripts, trim devDeps)
- [x] Copy tsconfig.json
- [x] Add "derive" to pnpm-workspace.yaml
- [x] Copy original worklog into .notes/justin/worklogs/
- [x] Run pnpm install at root
- [x] Verify typecheck passes

---

## Completed migration (2026-03-03)

All tasks complete. `pnpm --filter derive typecheck` passes cleanly via `tsgo`.

### What was done

1. **Copied** all 6 source files (`db.ts`, `index.ts`, `reader.ts`, `spec.ts`, `types.ts`, `watcher.ts`) from `machinen-experiments_specs/specs/src/` into `derive/src/`.
2. **Created** `derive/package.json` — renamed from `machinen` to `derive`, swapped `typecheck` script from `tsc --noEmit` to `tsgo`, removed `tsx` and `typescript` from devDependencies (available at root level).
3. **Copied** `tsconfig.json` unchanged — it already matched the `supervisor` package's conventions (`module: "NodeNext"`, `target: "ES2022"`, `strict: true`).
4. **Added** `"derive"` to `pnpm-workspace.yaml`.
5. **Copied** original worklog (`2026-03-03-machinen-setup.md`) into `.notes/justin/worklogs/`.
6. **Ran** `pnpm install --no-frozen-lockfile` — dependencies resolved cleanly (chokidar and execa already in lockfile from other packages).
7. **Verified** `pnpm --filter derive typecheck` passes with no errors.

### Not carried over

- `probe-real-sysprompt.mjs` — diagnostic script, not relevant to the migrated package.
- `pnpm-lock.yaml` from the source repo — the monorepo's own lockfile was updated.
