name: Container Test
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: ubuntu:24.04
    steps:
      - name: Check OS
        run: |
          echo "OS_RELEASE_START"
          cat /etc/os-release
          echo "OS_RELEASE_END"
