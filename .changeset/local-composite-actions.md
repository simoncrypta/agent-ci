---
"@redwoodjs/agent-ci": minor
"dtu-github-actions": minor
---

Support local composite actions (`uses: ./.github/actions/...`) by setting `RepositoryType: "self"` so the runner resolves them from the workspace.
